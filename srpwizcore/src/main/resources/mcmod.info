[
{
  "modid": "srpwizcore",
  "name": "SRP&WIZ Core",
  "description": "Pack-glue and worldgen control for heavy 1.12.2 packs: EntityTracker, MapStorage and IntCache concurrency fixes (MapStorage stops a torn world-data list from silently truncating the world save; IntCache gives every thread its own GenLayer array pools so concurrent chunk generation cannot corrupt biome data), OpenTerrainGenerator structure-gen null-biome guards, FutureMC bamboo worldgen race guard, per-dimension Ice&Fire worldgen control, perf-glue guards for Doomlike Dungeons (skips dungeon plans with a null map instead of throwing an NPE per chunk), Chocolate Quest Repoured (skips structure ring scans that can never succeed in OTG dimensions) and Raids-Backport (registers its world data in the per-world storage it is read from, killing a per-tick disk stat), plus a native configurable dormant-waystone travel system (block/worldgen/channelled teleport) and a per-dimension spawn engine (total and per-namespace population budgets, spawn candidate-list filtering before any entity is constructed, and a token-bucket refill rate limit so a cleared area stays quiet instead of being refilled on the next pass). Every feature independently config-gated.",
  "version": "1.6.2",
  "mcversion": "1.12.2",
  "authorList": ["Isuthhh"],
  "url": "https://www.curseforge.com/minecraft/mc-mods/srpwiz-core",
  "dependencies": []
}
]
